<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.blog">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Blog">
        <activity
            android:name=".BlogActivities.postDetailActivity"
            android:exported="false" />
        <activity
            android:name=".BlogActivities.Activity.Home"
            android:exported="false"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.Blog.NoActionBar" />
        <activity
            android:name=".BlogActivities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BlogActivities.HomeActivity"
            android:exported="true" />
        <activity
            android:name=".BlogActivities.RegisterActivity"
            android:exported="true" />
    </application>

</manifest>